# kanat-panel
Telegram WebApp UI (панель управления) для KanatnyiRoker.

## Что делает
- Открывается внутри Telegram.
- Кнопки отправляют команды в TG-бота через `Telegram.WebApp.sendData(JSON)`.

Команды: `start`, `stop`, `restart`, `status`, `about`, `copy_ip`.
